import React from "react";
import { Box, Heading, Text } from "@chakra-ui/react";

export default function Settings() {
  return (
    <Box>
      <Heading size="lg" mb={4}>الإعدادات</Heading>
      <Text>إعدادات عامة للطبيب (قريباً).</Text>
    </Box>
  );
}
